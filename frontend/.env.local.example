# Pipeline VizAPI base URL (docker compose maps host 8081 → container 8080)
TELEMETRY_API_URL=http://localhost:8081

# Server-only tenant API keys (Next.js proxy adds X-API-Key — never sent to browser)
TENANT_API_KEYS={"acme":"acme-docker-key","globex":"globex-docker-key","default":""}