apiVersion: v1
kind: Service
metadata:
  name: telemetry-pipeline
  namespace: telemetry
  labels:
    app: telemetry-pipeline
spec:
  type: ClusterIP
  selector:
    app: telemetry-pipeline
  ports:
    - name: http
      port: 8080
      targetPort: http